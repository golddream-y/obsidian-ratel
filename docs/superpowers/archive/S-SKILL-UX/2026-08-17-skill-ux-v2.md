# P-SKILL-UX-V2 — Skill 体验对齐 Claude/Cursor 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删 Skill 总开关(装了就生效)、管理入口对齐 MCP 抽屉 Modal、用户侧术语去「激活」、activation 收敛 `auto|manual`、per-skill 开关持久化到 `settings.skillEnabled`。

**Architecture:** 表达层简化、机制层架构不动(ADR-012 Discovery + activate_skill 写会话保留)。4 个 Task 按依赖排序:机制收敛 → 表达清理 → 管理 Modal → 文档修订。同 Task 内改动一次 commit(用户偏好 feature 分组,不碎切)。

**Tech Stack:** TypeScript(strict)、Svelte 5、Obsidian API、Vitest。

**所属 Spec:** [S-SKILL-UX](../specs/2026-08-17-skill-ux-claude-aligned-design.md)

**关键背景(subagent 必读):**
- 斜杠 `/skill` `/skills` `/skill off` 三条命令在 `ChatView.executeSlashCommand` 里**本就没有 handler**(死入口),只删 `slash-commands.ts` 定义即可,ChatView 无需清理。
- `skill.notice.activating`、`skill.cmd.reloadSkills`、`skill.active.title`、`skill.discovery.title/empty` 疑似死 key(删前每个都要 grep 验证零引用)。
- `SkillActivator.composeActive()` 疑似死代码(ADR-012 落地后 Active 段不再注入,`main.ts` 传死值 `''`),删前 grep 验证。
- vault 源 skill 的 `dir` 是 vault 相对路径(`.ratel/skills/<name>`);builtin/global 源是绝对路径。
- `ObsidianVault.resolveFile` 只认 `TFile`,删 vault 技能文件夹需新增 `trashFolder`。

---

## 文件结构总览

| 文件 | 动作 | 职责 |
|---|---|---|
| `src/skills/types.ts` | 改 | `SkillActivation` 收敛两态 |
| `src/skills/skill-loader.ts` | 改 | `always` 降级 auto + warning |
| `src/skills/skill-registry.ts` | 改 | 删 `getAlwaysSkills`;增 `applyEnabledOverrides` |
| `src/skills/skill-activator.ts` | 改 | 删死代码 `composeActive`(验证后) |
| `src/core/context-manager.ts` | 改 | 删 `ensureAlwaysSkillsInjected` |
| `src/core/agent-loop.ts` | 改 | 删 always 注入调用 + `skillRegistry` 参数 |
| `src/main.ts` | 改 | 删 3 处 enableSkills 条件 + reloadSkills 短路;增 skillEnabled 应用;增 `openSkillManageModal` |
| `src/settings.ts` | 改 | 删 `enableSkills` 类型/默认/设置组;增 `skillEnabled` |
| `src/settings/config-whitelist.ts` | 改 | 删 2 处 `enableSkills` |
| `src/ui/chat/input/slash-commands.ts` | 改 | 删 3 条命令 |
| `src/i18n/types.ts`、`zh.ts`、`en.ts` | 改 | 删激活类 key;话术改「使用」;增 Modal key |
| `src/prompts/defaults/zh.ts` | 改 | `agent.skills` 模板去「激活」 |
| `src/ui/skills/SkillManageModal.ts` | **新** | 技能管理 Modal(对齐 McpManageModal) |
| `src/adapters/obsidian-vault.ts` | 改 | 增 `trashFolder` |
| `src/ui/status/StatusDrawer.svelte` | 改 | 增「技能」按钮 |
| `src/ui/chat/ChatView.svelte` | 改 | 接线 `onSkill` |
| `docs/adr/2026-07-06-skill-mechanism.md` | 改 | §4 修订记录 |
| `docs/adr/2026-07-23-skill-activation-claude-aligned.md` | 改 | §3/§5 修订记录 |
| `docs/architecture/agent/capability-surface.md` | 改 | 去 always 提法 |
| `docs/architecture/host/settings.md` | 改 | 分区图 Skills 标签 |

---

### Task 1: 机制层收敛 — activation 两态 + always 链路删除 + skillEnabled 持久化

**Files:**
- Modify: `src/skills/types.ts:19-25`
- Modify: `src/skills/skill-loader.ts:151-168`
- Modify: `src/skills/skill-registry.ts`
- Modify: `src/skills/skill-activator.ts:62-92`
- Modify: `src/core/context-manager.ts:389-398`
- Modify: `src/core/agent-loop.ts:72-100`
- Modify: `src/main.ts`(onload 281-285、ask 1296-1319、reloadSkills 1625-1650、agentLoop 调用 1357-1370)
- Modify: `src/settings.ts`(interface 149-150、defaults 278-279 — 本 Task 只增 `skillEnabled`,`enableSkills` 删除留给 Task 2,避免组引用编译错)
- Test: `src/skills/skill-loader.test.ts`、`src/skills/skill-registry.test.ts`、`src/skills/skill-activator.test.ts`、`src/tools/activate-skill.test.ts`(仅回归)

- [ ] **Step 1: 写失败测试 — loader 对 always 降级**

在 `src/skills/skill-loader.test.ts` 追加(放在现有 activation 用例旁;文件已有 makePort/matter 风格 helper,沿用同款):

```typescript
it('normalizeActivation - always 已废弃 - 降级 auto 并记 warning', async () => {
	const port = makePortWithSkill('legacy-skill', '---
name: legacy-skill
description: 测试 always 废弃
activation: always
---
正文');
	const loader = new SkillLoader([port]);
	const { skills, warnings } = await loader.loadAll();
	expect(skills[0]?.manifest.activation).toBe('auto');
	expect(warnings.some((w) => w.message.includes('always'))).toBe(true);
});
```

> `makePortWithSkill` 若不存在,照抄该文件现有构造单端口 stub 的写法(`readSkillManifest` 返回上面的 frontmatter 字符串、`listSkillFolders` 返回 `['legacy-skill']`)。

- [ ] **Step 2: 写失败测试 — registry 删 always、增 applyEnabledOverrides**

`src/skills/skill-registry.test.ts`:

```typescript
it('reload - always 类型不再有专属查询 - getAlwaysSkills 已删除', () => {
	// S-SKILL-UX:always 废弃,Registry 不再暴露 always 查询。
	expect((registry as unknown as Record<string, unknown>).getAlwaysSkills).toBeUndefined();
});

it('applyEnabledOverrides - 全量应用并影响 isEnabled', () => {
	registry.reload([makeSkill('a'), makeSkill('b')], []);
	registry.applyEnabledOverrides({ a: false });
	expect(registry.isEnabled('a')).toBe(false);
	expect(registry.isEnabled('b')).toBe(true);
});
```

同时删除既有用例 `it('reload - always 类型进入 getAlwaysSkills 但不进 getActive', ...)`(约 35-43 行)。

- [ ] **Step 3: 跑测试确认失败**

```bash
npx vitest run src/skills/skill-loader.test.ts src/skills/skill-registry.test.ts
```

预期:新增 2 用例 FAIL(always 未降级 / applyEnabledOverrides 不存在),既有 always 用例尚 PASS(实现未动)。

- [ ] **Step 4: 实现 — types.ts 收敛**

```typescript
/**
 * Skill 激活模式(来自 SKILL.md frontmatter `activation` 字段)。
 *
 * - `auto`:LLM 自主判断任务匹配后调用(默认,可省略)
 * - `manual`:不进 Discovery 段,仅用户点名时由模型调用
 *
 * S-SKILL-UX:`always` 已废弃 — loader 按 `auto` 降级加载并记 warning。
 */
export type SkillActivation = 'auto' | 'manual';
```

- [ ] **Step 5: 实现 — loader 降级**

`skill-loader.ts` 的 `normalizeActivation` 整体替换为:

```typescript
	/**
	 * 规范化 activation 字段(S-SKILL-UX:两态)。
	 *
	 * 关键路径:`always` 已废弃 — 按 `auto` 降级加载并记 warning,
	 * 保证旧 frontmatter 不阻塞加载;其余非法值同样降级 auto。
	 */
	private normalizeActivation(
		value: unknown,
		skillPath: string,
		warnings: SkillLoadWarning[],
	): SkillActivation {
		if (value === 'always') {
			warnings.push({ path: skillPath, message: 'activation "always" 已废弃,按 auto 加载' });
			return 'auto';
		}
		if (value === 'auto' || value === 'manual') return value;
		if (value !== undefined) {
			const shown =
				typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean'
					? String(value)
					: typeof value;
			warnings.push({ path: skillPath, message: `activation 非法值 "${shown}",降级 auto` });
		}
		return 'auto';
	}
```

同时更新文件头 JSDoc 与 `buildManifest` 注释里的「activation 非法值降级 'auto'」描述为「always/非法值降级 'auto'」(仅注释,无逻辑)。

- [ ] **Step 6: 实现 — registry 删 always、增 override 同步**

`skill-registry.ts`:

1. 删除 `getAlwaysSkills()` 方法(66-72 行)及其 JSDoc。
2. 在 `setEnabled` 之后新增:

```typescript
	/**
	 * 从 settings.skillEnabled 全量应用 per-skill 开关(S-SKILL-UX)。
	 *
	 * 关键路径:reloadSkills 每次加载后调用,持久化状态覆盖内存 Map;
	 * settings 里未登记的 skill 走 manifest.enabled 默认值。
	 *
	 * @param record - settings.skillEnabled(name → 是否启用)
	 */
	applyEnabledOverrides(record: Record<string, boolean>): void {
		this.enabledOverrides = new Map(Object.entries(record));
	}
```

3. 类头 JSDoc 三态描述更新:删 `always` 一行,`enabled` 行改为「per-skill 开关,持久化在 settings.skillEnabled,Modal toggle 后立即生效」;`reload` JSDoc 删「activation='always' 的 skill 自动加入 activeSkills」与 55 行 ADR-012 always 注释行。
4. `reload()` 里 55 行注释 `// ADR-012:always 不再写入...` 删除。

- [ ] **Step 7: 实现 — 删 context-manager / agent-loop 的 always 链路**

`src/core/context-manager.ts`:删除 `ensureAlwaysSkillsInjected` 方法(389-398 行,含 JSDoc)。`appendSkillInstructions` **保留**(activate_skill 的 sessionHooks 仍用)。

`src/core/agent-loop.ts`:

1. 删除 94-97 行:

```typescript
	// ADR-012:always skill 按场一次性写入 messages;Discovery 仍走 setSkillsContext。
	if (skillRegistry) {
		ctx.ensureAlwaysSkillsInjected(skillRegistry.getAlwaysSkills());
	}
```

2. 删除签名中的 `skillRegistry?: SkillRegistry` 参数(82-84 行注释一并改):「注入 Activator(Discovery 重组用)」。同步删 `import type { SkillRegistry }`(若仅此一处使用)。

- [ ] **Step 8: 实现 — main.ts 删条件 + 应用持久化开关**

1. onload(281-285 行)替换为:

```typescript
		// 关键路径:onload 异步加载 skills,不阻塞 Obsidian 启动(spec §6.6)。
		// S-SKILL-UX:技能装了就生效,无条件加载。
		void this.reloadSkills();
```

2. `ask()` 内 getSkillsDiscovery(1300-1305 行)改为:

```typescript
			// ADR-012:仅 Discovery;Active 指令写入 Session.messages。
			getSkillsDiscovery: () =>
				this.skillActivator.composeDiscovery(this.settings.promptOverrides),
```

3. `ask()` 内 setSkillsContext 块(1313-1319 行)改为:

```typescript
		// 关键路径:会话启动时注入 Discovery 目录(activate 写进 messages)。
		ctx.setSkillsContext(
			this.skillActivator.composeDiscovery(this.settings.promptOverrides),
			'',
		);
```

4. `agentLoop` 调用(1357-1370 行)删 `this.skillRegistry,` 一行(1368)。
5. `reloadSkills()`(1625-1650 行):删 1629 注释行与 1636 短路行;`this.skillRegistry.reload(skills, warnings);` 之后新增一行:

```typescript
		// S-SKILL-UX:应用持久化的 per-skill 开关(settings.skillEnabled)。
		this.skillRegistry.applyEnabledOverrides(this.settings.skillEnabled);
```

6. JSDoc「enableSkills=false 时直接返回 0」描述删除。

- [ ] **Step 9: 实现 — settings.ts 增 skillEnabled(本 Task 不删 enableSkills)**

interface(149-150 行)改为:

```typescript
	// 关键路径(S-SKILL-UX):per-skill 开关持久化(name → 是否启用)。
	// 未登记的 skill 走 manifest.enabled 默认值;Registry 加载后 applyEnabledOverrides 应用。
	skillEnabled: Record<string, boolean>;
	// 旧 enableSkills 已废弃,S-SKILL-UX 删除(本 Task 先保留定义,Task 2 连设置组一起删)。
	enableSkills: boolean;
```

DEFAULT_SETTINGS(278-279 行)改为:

```typescript
	// 关键路径(S-SKILL-UX):per-skill 开关持久化,默认空(全部走 manifest.enabled)。
	skillEnabled: {},
	enableSkills: true,
```

- [ ] **Step 10: 删 composeActive 死代码(先验证)**

```bash
grep -rn "composeActive\|skill.active.title\|skill.discovery.title\|skill.discovery.empty\|skill.notice.activating" src/ --include="*.ts" --include="*.svelte"
```

预期:仅 `skill-activator.ts`(定义)、`i18n/{types,zh,en}.ts`(key 定义)、测试文件命中,无业务调用方。若成立:

1. 删 `skill-activator.ts` 的 `composeActive()` 方法(62-81 行)与 `import { tNow }`(若 resolveDescription 不再用;它不用 tNow,删)。
2. 类头 JSDoc 第 2 点「Active 段」描述改为「(已废弃,S-SKILL-UX 删除;指令经 activate_skill 写入 Session.messages,见 ADR-012)」。
3. `skill-activator.test.ts` 删 composeActive 相关用例。
4. i18n key 删除归 Task 2 一起做(本步只删代码)。

若有调用方:保留 composeActive,在执行日志记录偏差。

- [ ] **Step 11: 跑全量测试 + 类型检查**

```bash
npx vitest run && npx tsc --noEmit
```

预期:全 PASS。若 `agent-loop.test` / `context-manager.test` 有传 `skillRegistry` 或断言 always 注入的用例,同步删除(用例名含 always / ensureAlwaysSkillsInjected)。

- [ ] **Step 12: Commit**

```bash
git add -A && git commit -m "$(cat <<'EOF'
feat: skill 装了就生效 — activation 收敛两态,always 链路与开关短路删除(S-SKILL-UX)

activation=always 的旧技能每次会话都要走专属注入链路,且 settings 总开关
把加载短路后Discovery 段悄悄消失,用户无法理解。现收敛为 auto|manual
两态(always 降级 auto+warning),删 getAlwaysSkills/ensureAlwaysSkillsInjected
全链路,main 三处 enableSkills 条件删除,per-skill 开关持久化到
settings.skillEnabled 并在每次 reload 后应用。
EOF
)"
```

---

### Task 2: 表达层清理 — 设置组/白名单/斜杠/i18n/prompt 话术

**Files:**
- Modify: `src/settings.ts`(组 739-751、interface 149-152、defaults 278-280)
- Modify: `src/settings/config-whitelist.ts:48,112`
- Modify: `src/ui/chat/input/slash-commands.ts:49-63`
- Modify: `src/i18n/types.ts`、`src/i18n/zh.ts`、`src/i18n/en.ts`(skill 块)
- Modify: `src/prompts/defaults/zh.ts:56-62`
- Test: `tests/settings/config-whitelist.test.ts:52-53`、`tests/ui/slash-commands.test.ts:12-14`、`src/tools/activate-skill.test.ts`、`src/tools/deactivate-skill.test.ts`

- [ ] **Step 1: 写失败测试 — 斜杠命令收敛为 4 条**

`tests/ui/slash-commands.test.ts` 12-14 行改为:

```typescript
	it('getSlashCommands - 含 4 个命令(new/compact/model/reindex)', () => {
		const names = getSlashCommands().map((c) => c.name);
		expect(names).toEqual(['/new', '/compact', '/model', '/reindex']);
	});
```

`tests/settings/config-whitelist.test.ts` 52-53 行改为:

```typescript
			// 日记
			'dailyNoteFolder', 'dailyNoteFormat',
```

`src/tools/activate-skill.test.ts` 断言 `已激活` 的两处(46、60 行)改为断言 `已使用技能`。

- [ ] **Step 2: 跑测试确认失败**

```bash
npx vitest run tests/ui/slash-commands.test.ts tests/settings/config-whitelist.test.ts src/tools/activate-skill.test.ts
```

预期:3 个文件 FAIL(命令仍 7 条 / 白名单仍含 enableSkills / 文案未改)。

- [ ] **Step 3: 实现 — settings.ts 删组与字段**

1. 删「Skill 管理」组(739-751 行整块:`{ type: 'group', heading: tNow('skill.settings.heading'), ... }`)。
2. interface:删 Task 1 留下的 `enableSkills: boolean;` 与其上「旧 enableSkills 已废弃」注释行,`skillEnabled` 注释保留。
3. DEFAULT_SETTINGS:删 `enableSkills: true,` 行。

- [ ] **Step 4: 实现 — 白名单 2 处**

`config-whitelist.ts`:48 行 `'enableSkills',` 删除(注释 `// --- Skill / 日记 ---` 改为 `// --- 日记 ---`);112 行 `'enableSkills',` 从 `BOOLEAN_KEYS` 删除。

- [ ] **Step 5: 实现 — 斜杠 3 条删除**

`slash-commands.ts`:删 49-63 行三条 `/skill` `/skills` `/skill off` 定义。文件头注释如有命令数描述同步改。

- [ ] **Step 6: 实现 — i18n 三件套**

`src/i18n/types.ts` SkillStrings 删以下 key 声明,新增 Modal/抽屉 key(Task 3 一并加,本 Task 先删):

删除:`skill.settings.heading`、`skill.settings.enableSkills.name`、`skill.settings.enableSkills.desc`、`skill.cmd.skill`、`skill.cmd.skills`、`skill.cmd.reloadSkills`、`skill.activation.always`、`skill.notice.activating`、`skill.active.title`、`skill.discovery.title`、`skill.discovery.empty`(每个删前 grep 确认零引用;`skill.cmd.reloadSkills` 与 activating 为死 key)。

`src/i18n/zh.ts` skillZh 块同步删上述 key,并改话术:

```typescript
  // Notice(S-SKILL-UX:用户侧术语用「使用」,不再出现「激活」)
  'skill.notice.activated': '已使用技能 {name}',
  'skill.notice.deactivated': '已停用技能 {name}',
  'skill.notice.notFound': '未找到技能: {name}',
  'skill.notice.alreadyActive': '技能 {name} 已在当前会话生效',
  'skill.notice.notActive': '技能 {name} 未在使用',
  'skill.notice.reloadDone': '已重新加载 {count} 个技能',
  'skill.notice.reloadFailed': '技能重新加载失败: {message}',
```

`src/i18n/en.ts` 对应:

```typescript
  'skill.notice.activated': 'Using skill {name}',
  'skill.notice.deactivated': 'Skill {name} disabled',
  'skill.notice.notFound': 'Skill not found: {name}',
  'skill.notice.alreadyActive': 'Skill {name} already in effect for this chat',
  'skill.notice.notActive': 'Skill {name} not in use',
  'skill.notice.reloadDone': 'Reloaded {count} skills',
  'skill.notice.reloadFailed': 'Skill reload failed: {message}',
```

(中英两文件的来源标签 `skill.source.*`、`skill.activation.auto/manual`、`error.skill.*`、`cmd.reloadSkills` 保留。)

- [ ] **Step 7: 实现 — Discovery 模板话术**

`src/prompts/defaults/zh.ts` 56-62 行替换为:

```typescript
	// 关键路径:Skill Discovery 段 — 注入已加载 skill 的 name+description 列表。
	// S-SKILL-UX:话术对齐渐进披露 — 任务匹配时读取该技能的完整做法。
	// 占位符:{{skillList}} = skill 列表行("- name: description" 格式)。
	'agent.skills': `## 可用技能

以下技能已加载。当任务与某技能的描述匹配时,调用 \`activate_skill(name)\` 读取该技能的完整做法,并按其步骤执行。

{{skillList}}`,
```

- [ ] **Step 8: 跑全量测试 + 构建**

```bash
npx vitest run && npx tsc --noEmit && npm run build
```

预期:全 PASS;构建 0 error(存量 Svelte legacy warning 不计)。

- [ ] **Step 9: Commit**

```bash
git add -A && git commit -m "$(cat <<'EOF'
feat: 用户侧不再出现「激活技能」— 斜杠/设置组/总开关全删,话术改「使用」(S-SKILL-UX)

/skill 三条斜杠本是死入口(选中无响应),设置页总开关关掉后 Discovery
段静默消失,「激活」术语用户无法理解。删 3 条斜杠与「Skill 管理」设置组,
enableSkills 白名单 2 处移除,聊天文案改「已使用技能」,Discovery 模板
改为「任务匹配时读取该技能的完整做法」。
EOF
)"
```

---

### Task 3: SkillManageModal + 抽屉接线(对齐 McpManageModal)

**Files:**
- Create: `src/ui/skills/SkillManageModal.ts`
- Modify: `src/adapters/obsidian-vault.ts`(增 `trashFolder`)
- Modify: `src/ui/status/StatusDrawer.svelte`(props 18-34、按钮区 151-188)
- Modify: `src/ui/chat/ChatView.svelte`(openMcp 旁 920-923、StatusDrawer 用法 1533)
- Modify: `src/main.ts`(import 区、字段区、openMcpManageModal 旁 1610-1623)
- Modify: `src/i18n/types.ts`、`src/i18n/zh.ts`、`src/i18n/en.ts`(新增 key)
- Test: 无独立单测(UI 走沙盒手测;`shouldCreateSkillManageModal` 纯函数可加轻量断言进现有任意 UI 测试文件,见 Step 6)

- [ ] **Step 1: i18n 新增 key(先写,Modal 引用)**

`src/i18n/types.ts` SkillStrings 增:

```typescript
  // StatusDrawer 入口(S-SKILL-UX)
  'status.drawer.skill': string;
  // SkillManageModal(S-SKILL-UX)
  'modal.skillManage.title': string;
  'modal.skillManage.installedHeading': string;
  'modal.skillManage.empty': string;
  'modal.skillManage.emptyHint': string;
  'modal.skillManage.reload': string;
  'modal.skillManage.viewFull': string;
  'modal.skillManage.builtinReadonly': string;
  'modal.skillManage.edit': string;
  'modal.skillManage.delete': string;
  'modal.skillManage.confirmDelete': string;
  'modal.skillManage.deleted': string;
  'modal.skillManage.deleteFailed': string;
  'modal.skillManage.editHint': string;
```

> 若 `status.drawer.*` key 声明在别的 namespace interface(如 StatusStrings),`status.drawer.skill` 加到那边 — 以现有 `status.drawer.mcp` 的声明位置为准。

`src/i18n/zh.ts`:

```typescript
  'status.drawer.skill': '技能',
  'modal.skillManage.title': '技能管理',
  'modal.skillManage.installedHeading': '已安装技能({count})',
  'modal.skillManage.empty': '还没有技能',
  'modal.skillManage.emptyHint': '把 SKILL.md 放进库内 .ratel/skills/<技能名>/ 即可启用;也支持全局 ~/.ratel/skills/ 与插件内置,库内同名优先。',
  'modal.skillManage.reload': '重新扫描',
  'modal.skillManage.viewFull': '查看全文',
  'modal.skillManage.builtinReadonly': '内置技能,随插件更新',
  'modal.skillManage.edit': '编辑',
  'modal.skillManage.delete': '删除',
  'modal.skillManage.confirmDelete': '再点一次确认删除',
  'modal.skillManage.deleted': '已删除技能 {name}',
  'modal.skillManage.deleteFailed': '删除失败: {message}',
  'modal.skillManage.editHint': '技能目录: {path}',
```

`src/i18n/en.ts`:

```typescript
  'status.drawer.skill': 'Skills',
  'modal.skillManage.title': 'Manage skills',
  'modal.skillManage.installedHeading': 'Installed skills ({count})',
  'modal.skillManage.empty': 'No skills yet',
  'modal.skillManage.emptyHint': 'Drop a SKILL.md into .ratel/skills/<name>/ inside your vault; global ~/.ratel/skills/ and built-in sources also work. Vault wins on name conflicts.',
  'modal.skillManage.reload': 'Rescan',
  'modal.skillManage.viewFull': 'View full text',
  'modal.skillManage.builtinReadonly': 'Built-in skill; updated with the plugin',
  'modal.skillManage.edit': 'Edit',
  'modal.skillManage.delete': 'Delete',
  'modal.skillManage.confirmDelete': 'Click again to confirm',
  'modal.skillManage.deleted': 'Deleted skill {name}',
  'modal.skillManage.deleteFailed': 'Delete failed: {message}',
  'modal.skillManage.editHint': 'Skill folder: {path}',
```

- [ ] **Step 2: 外观增 trashFolder**

`src/adapters/obsidian-vault.ts`:文件头 obsidian import 增加 `TFolder`(现有 `TFile` 旁);`trashFile` 方法之后新增:

```typescript
	/**
	 * 把文件夹移入回收站(尊重用户删除偏好;S-SKILL-UX 供 SkillManageModal 删库内技能目录)。
	 *
	 * @param path - vault 相对文件夹路径(如 `.ratel/skills/<name>`)
	 * @throws 路径不存在或不是文件夹时抛 i18n 错误
	 */
	async trashFolder(path: string): Promise<void> {
		const normalized = validateVaultPath(path);
		const folder = this.app.vault.getAbstractFileByPath(normalized);
		if (!folder || !(folder instanceof TFolder)) {
			throw new Error(tNow('error.tool.fileNotFound', { path: normalized }));
		}
		// 关键路径:fileManager.trashFile 同时接受 TFile|TFolder,自动走系统回收站/.trash。
		await this.app.fileManager.trashFile(folder);
	}
```

- [ ] **Step 3: 新建 SkillManageModal.ts**

```typescript
/**
 * @file src/ui/skills/SkillManageModal.ts
 * @description 技能安装与管理 Modal(状态抽屉主入口,对齐 McpManageModal 模式)
 * @module ui/skills/SkillManageModal
 * @depends obsidian, node:fs/promises, ../../i18n, ../../skills/skill-registry, ../../main
 */

import { App, Modal, Notice, Setting } from 'obsidian';
import { rm } from 'node:fs/promises';
import type RatelVaultPlugin from '../../main';
import { tNow } from '../../i18n';
import { devLogger } from '../../logging/dev-logger';

/**
 * 是否应新建 Modal — 已有实例则 false。
 *
 * @param current - 插件持有的当前单例引用
 */
export function shouldCreateSkillManageModal(current: SkillManageModal | null): boolean {
	return current === null;
}

/**
 * 技能管理 Modal — 三源技能的统一管理入口(S-SKILL-UX)。
 *
 * 设计要点:
 * - 列表只显示合并后实际生效的技能(vault > global > builtin),徽标标生效来源
 * - per-skill 开关持久化到 settings.skillEnabled,不碰 SKILL.md 源文件
 * - 三源权限:内置只读(随插件更新);库内可编辑(Obsidian 打开)/可删(回收站);
 *   全局可编辑(系统打开目录)/可删(rm -rf)
 * - 删除为两击确认(第一击变红提示,第二击执行),避免误删
 */
export class SkillManageModal extends Modal {
	/** 关闭时回调,供 plugin 清掉单例引用 */
	onClosed: (() => void) | null = null;
	/** 当前处于「再点一次确认删除」状态的技能名(两击确认) */
	private confirmDeleteName: string | null = null;

	constructor(
		app: App,
		private plugin: RatelVaultPlugin,
	) {
		super(app);
	}

	onOpen(): void {
		this.titleEl.setText(tNow('modal.skillManage.title'));
		this.modalEl.addClass('ratel-skill-manage-modal');
		this.renderBody();
	}

	onClose(): void {
		this.contentEl.empty();
		this.onClosed?.();
		this.onClosed = null;
	}

	private renderBody(): void {
		this.contentEl.empty();
		this.confirmDeleteName = null;
		const skills = [...this.plugin.skillRegistry.getAll()].sort((a, b) =>
			a.manifest.name.localeCompare(b.manifest.name),
		);

		this.contentEl.createEl('h3', {
			text: tNow('modal.skillManage.installedHeading').replace(
				'{count}',
				String(skills.length),
			),
		});

		if (skills.length === 0) {
			this.contentEl.createEl('p', { text: tNow('modal.skillManage.empty') });
			this.contentEl.createEl('p', {
				cls: 'setting-item-description',
				text: tNow('modal.skillManage.emptyHint'),
			});
		} else {
			for (const skill of skills) {
				this.renderSkillRow(skill.manifest.name, skill.source, skill.dir);
			}
		}

		new Setting(this.contentEl).addButton((b) =>
			b.setButtonText(tNow('modal.skillManage.reload')).onClick(async () => {
				await this.plugin.reloadSkills();
				new Notice(tNow('skill.notice.reloadDone').replace('{count}', String(this.plugin.skillRegistry.getAll().length)));
				this.renderBody();
			}),
		);
	}

	/**
	 * 渲染单个技能行 — 名称/描述/来源徽标 + 开关 + 按来源权限的操作按钮。
	 *
	 * 关键路径:正文已在 Registry 内存里(instructions),查看全文不读文件系统。
	 */
	private renderSkillRow(name: string, source: 'builtin' | 'global' | 'vault', dir: string): void {
		const registry = this.plugin.skillRegistry;
		const skill = registry.get(name);
		if (!skill) return;

		const desc =
			`${tNow(`skill.source.${source}`)}` +
			(skill.manifest.version ? ` · v${skill.manifest.version}` : '') +
			(source === 'builtin' ? ` · ${tNow('modal.skillManage.builtinReadonly')}` : '');

		const row = new Setting(this.contentEl)
			.setName(name)
			.setDesc(desc);

		// 查看全文:details/summary 只读展开(SKILL.md 正文,frontmatter 之后的 instructions)
		const details = this.contentEl.createEl('details');
		details.createEl('summary', { text: tNow('modal.skillManage.viewFull') });
		const pre = details.createEl('pre');
		pre.setText(skill.instructions);

		row.addToggle((tog) => {
			tog.setValue(registry.isEnabled(name)).onChange(async (v) => {
				// 关键路径:开关持久化到 settings.skillEnabled,禁用时 Registry 清 active。
				this.plugin.settings.skillEnabled[name] = v;
				registry.setEnabled(name, v);
				await this.plugin.saveSettings();
			});
		});

		if (source === 'vault') {
			row.addButton((b) =>
				b.setButtonText(tNow('modal.skillManage.edit')).onClick(() => {
					// 有 open-chat-note.ts 先例:UI 层直接 openLinkText(vault 相对路径)。
					void this.app.workspace.openLinkText(`${dir}/SKILL.md`, '', false);
				}),
			);
			row.addButton((b) => this.wireDeleteButton(b, name, async () => {
				await this.plugin.vault.trashFolder(dir);
			}));
		} else if (source === 'global') {
			row.addButton((b) =>
				b.setButtonText(tNow('modal.skillManage.edit')).onClick(() => {
					this.openInSystem(dir);
				}),
			);
			row.addButton((b) => this.wireDeleteButton(b, name, async () => {
				await rm(dir, { recursive: true, force: true });
			}));
		}
		// builtin:无编辑/删除(升级幂等重写,编辑必被覆盖)— desc 已注明。
	}

	/**
	 * 两击确认删除 — 第一击切红色「再点一次确认删除」,第二击执行后重扫描。
	 *
	 * @param b - 按钮组件
	 * @param name - 技能名
	 * @param remove - 来源对应的实际删除动作(vault 回收站 / global rm)
	 */
	private wireDeleteButton(
		b: import('obsidian').ButtonComponent,
		name: string,
		remove: () => Promise<void>,
	): void {
		b.setButtonText(tNow('modal.skillManage.delete'))
			.setDestructive()
			.onClick(async () => {
				if (this.confirmDeleteName !== name) {
					// 第一击:进入确认态
					this.confirmDeleteName = name;
					b.setButtonText(tNow('modal.skillManage.confirmDelete'));
					return;
				}
				this.confirmDeleteName = null;
				try {
					await remove();
					await this.plugin.reloadSkills();
					delete this.plugin.settings.skillEnabled[name];
					await this.plugin.saveSettings();
					new Notice(tNow('modal.skillManage.deleted').replace('{name}', name));
					this.renderBody();
				} catch (err) {
					const message = err instanceof Error ? err.message : String(err);
					devLogger.error('skill', `删除技能 ${name} 失败`, err);
					new Notice(tNow('modal.skillManage.deleteFailed').replace('{message}', message));
				}
			});
	}

	/**
	 * 系统文件管理器打开全局技能目录(Electron shell;仅桌面端可用)。
	 * 失败降级为 Notice 显示路径,用户自行前往。
	 */
	private openInSystem(dir: string): void {
		try {
			const electron = (window as unknown as { require?: (m: string) => { shell: { openPath: (p: string) => Promise<string> } } }).require?.('electron');
			if (electron) {
				void electron.shell.openPath(dir);
				return;
			}
		} catch (err) {
			devLogger.warn('skill', 'electron shell 不可用,降级显示路径', err);
		}
		new Notice(tNow('modal.skillManage.editHint').replace('{path}', dir));
	}
}
```

> 样式:`ratel-skill-manage-modal` 宽度类与 `details/pre` 表现如需微调,加进 `styles.css`(圆角 ≤8px、禁 box-shadow、`ratel-` 前缀)。McpManageModal 的 `.ratel-mcp-manage-modal` 已有先例,复用同款宽度声明即可。

- [ ] **Step 4: main.ts 接线单例**

1. import 区(McpManageModal import 旁)加:

```typescript
import { SkillManageModal, shouldCreateSkillManageModal } from './ui/skills/SkillManageModal';
```

2. 字段区(`mcpManageModal` 声明旁)加:

```typescript
	/** Skill 管理 Modal 单例(S-SKILL-UX;已打开则忽略重复打开) */
	private skillManageModal: SkillManageModal | null = null;
```

3. `openMcpManageModal()`(1610-1623)之后加:

```typescript
	/**
	 * 打开技能管理 Modal(单例,S-SKILL-UX — 状态抽屉「技能」入口)。
	 *
	 * 关键路径:已打开则忽略,避免叠多个管理窗。
	 */
	openSkillManageModal(): void {
		if (!shouldCreateSkillManageModal(this.skillManageModal)) return;
		const modal = new SkillManageModal(this.app, this);
		this.skillManageModal = modal;
		modal.onClosed = () => {
			if (this.skillManageModal === modal) this.skillManageModal = null;
		};
		modal.open();
	}
```

- [ ] **Step 5: StatusDrawer + ChatView 接线**

`StatusDrawer.svelte`:

1. props 解构(18-25 行)加 `onSkill,`;interface(31-34 行 `onMcp` 之后)加:

```typescript
		/** 技能管理入口(可选,S-SKILL-UX) */
		onSkill?: () => void;
```

2. 条件(151 行)改为 `{#if onFeedback || onMemory || onMcp || onSkill || onSponsor}`。
3. `onMcp` 按钮块(168-188 行)之后加:

```svelte
				{#if onSkill}
					<button type="button" class="ratel-drawer-action" onclick={onSkill}>
						<svg class="ratel-drawer-action-ico" viewBox="0 0 24 24" width="13" height="13" aria-hidden="true">
							<path
								d="M12 3l1.9 5.1L19 10l-5.1 1.9L12 17l-1.9-5.1L5 10l5.1-1.9L12 3z"
								fill="none"
								stroke="currentColor"
								stroke-width="1.75"
								stroke-linejoin="round"
							/>
						</svg>
						<span>{$t('status.drawer.skill')}</span>
					</button>
				{/if}
```

`ChatView.svelte`:

1. `openMcp()`(920-923)之后加:

```typescript
	/** 状态抽屉「技能」入口 → 打开 SkillManageModal(S-SKILL-UX) */
	function openSkill(): void {
		plugin.openSkillManageModal();
	}
```

2. StatusDrawer 用法(1533 行 `onMcp={openMcp}` 旁)加 `onSkill={openSkill}`。

- [ ] **Step 6: 轻量断言 + 全量验证**

在 `tests/ui/` 任一现有测试文件(如 slash-commands.test.ts)追加:

```typescript
import { describe, expect, it } from 'vitest';
// 文件顶部补:import { shouldCreateSkillManageModal } from '../../src/ui/skills/SkillManageModal';

describe('shouldCreateSkillManageModal', () => {
	it('单例判定 - null 才新建', () => {
		expect(shouldCreateSkillManageModal(null)).toBe(true);
		expect(shouldCreateSkillManageModal({} as never)).toBe(false);
	});
});
```

```bash
npx vitest run && npx tsc --noEmit && npm run build
```

预期:全 PASS、构建成功。

- [ ] **Step 7: 沙盒手测清单(本机,路径见 .cursor/rules/*.local.mdc)**

Sandbox Reload app without saving 后逐项验证:

1. 设置页 Agent tab 无「Skill 管理」组
2. 状态抽屉出现「技能」按钮 → 打开 Modal
3. ratel-config 行:徽标「预置」、无编辑/删除、开关可切且重启保持
4. 库内放测试技能 `.ratel/skills/test-skill/SKILL.md`(frontmatter `activation: always`)→ 重扫描后 warning 出现、activation 显示 auto、开关/查看全文/编辑/删除两击确认可用
5. 斜杠输入 `/` 只见 4 条命令;自然语言「用 ratel-config 技能改xx」模型可 activate,trace 显示「已使用技能」

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "$(cat <<'EOF'
feat: 技能管理进抽屉 — SkillManageModal 对齐 MCP 模式,per-skill 开关落盘(S-SKILL-UX)

技能没有管理入口,开关只存在内存不落盘,重启即丢。新增状态抽屉「技能」
按钮 → SkillManageModal:三源徽标 + per-skill 开关(settings.skillEnabled
持久化)+ 查看全文 + 库内编辑/回收站删除 + 全局系统打开/rm 删除(两击确认),
内置只读随插件更新。外观新增 trashFolder 支持删库内技能目录。
EOF
)"
```

---

### Task 4: ADR 与架构文档修订

**Files:**
- Modify: `docs/adr/2026-07-06-skill-mechanism.md`(文末)
- Modify: `docs/adr/2026-07-23-skill-activation-claude-aligned.md`(文末)
- Modify: `docs/architecture/agent/capability-surface.md`
- Modify: `docs/architecture/host/settings.md:72,202`

- [ ] **Step 1: ADR-009 追加修订记录**

文末(`## 参考` 之后)追加:

```markdown
---

## 修订记录

| 日期 | 修订 | 来源 |
|---|---|---|
| 2026-08-17 | §4 三态收敛:`activation` 合法值收敛为 `auto\|manual`,`always` 废弃(loader 按 auto 降级 + warning),`getAlwaysSkills`/`ensureAlwaysSkillsInjected` 链路删除;§4 `enabled` 行「Settings 面板 toggle」从未实现,per-skill 开关实际持久化为 `settings.skillEnabled: Record<string, boolean>`,管理入口为状态抽屉 SkillManageModal | [S-SKILL-UX](../superpowers/specs/2026-08-17-skill-ux-claude-aligned-design.md) |
```

- [ ] **Step 2: ADR-012 追加修订记录**

文末(`## 参考` 之后)追加:

```markdown
---

## 修订记录

| 日期 | 修订 | 来源 |
|---|---|---|
| 2026-08-17 | §3 `always` 机制废弃:`activation` 收敛两态后 `always` 按 `auto` 降级加载,按场一次性注入链路(`ensureAlwaysSkillsInjected`)删除;§5「明确不改动的部分」中 `always` frontmatter 语义一条随之失效,`enabled` 开关迁移至 `settings.skillEnabled`(持久化)。Discovery + activate_skill 写会话的核心模型不变 | [S-SKILL-UX](../superpowers/specs/2026-08-17-skill-ux-claude-aligned-design.md) |
```

- [ ] **Step 3: capability-surface.md 更新**

```bash
grep -n "always\|激活" docs/architecture/agent/capability-surface.md
```

逐处处理:always 相关行删除或改为「activation 两态(auto|manual)」;「激活」保留于模型侧机制描述(activate_skill 是机制名),用户可见行为段落若提「激活技能」改为「使用技能」。§生命周期/供给方段落中 skill 开关描述改为「per-skill 开关持久化于 settings.skillEnabled,管理入口为状态抽屉 SkillManageModal(与 MCP 管理一致)」。

- [ ] **Step 4: settings.md 分区图**

72 行 `MEM["Memory / Skills / Daily"]` → `MEM["Memory / Daily"]`;202 行 `AGENT["记忆与权限:memory / skills / 日记 / trustMode / toolPermissions"]` → `AGENT["记忆与权限:memory / 日记 / trustMode / toolPermissions"]`。上下文若还有「Skill 管理设置组」文字描述,一并删。

- [ ] **Step 5: Commit**

```bash
git add docs/ && git commit -m "$(cat <<'EOF'
docs: 修订 ADR-009/012 与架构文档 — always 废弃、开关持久化迁移(S-SKILL-UX)

ADR-009 §4 三态收敛两态并纠正从未实现的「Settings 面板 toggle」描述;
ADR-012 §3 always 注入废弃、§5 不改动清单部分失效;capability-surface
与 settings 分区图同步(Skill 设置组已删,管理入口改抽屉 Modal)。
EOF
)"
```

---

## 自审记录

- **Spec 覆盖**:§4.1(设置组/斜杠/trace 话术)→ Task 2;§4.2 数据模型 → Task 1 Step 9 + Task 2 Step 3;§4.3 Modal → Task 3;§4.4 三源权限 → Task 3 Step 3;§4.5 机制层 → Task 1;§4.6 关系澄清不改 → 无任务(正确);§5.1 代码表 → Task 1-3 全覆盖(含测试);§5.2 架构文档 → Task 4;§5.3 用户文档 → finishing 阶段按 AGENTS.md 确认(user-guide /skill 行 128 待删,CHANGELOG [Unreleased] 补条目)。
- **占位符扫描**:无 TBD/TODO;所有代码步骤给全文;唯一条件项(composeActive 死代码删除)带 grep 验证门槛与偏差记录出口。
- **类型一致性**:`applyEnabledOverrides(record: Record<string, boolean>)` 在 Task 1 定义、Task 3 Modal 调用;`openSkillManageModal`/`shouldCreateSkillManageModal`/`trashFolder` 命名前后一致;`skillEnabled` 键名与 spec §4.2 一致。
- **顺序安全**:Task 1 保留 `enableSkills` 定义(设置组仍引用,编译安全);Task 2 才连组一起删。
