---
description: 本地预览必须确认当前 Obsidian 窗口链的是哪份 dist，禁止对着旧 worktree 改 UI
alwaysApply: true
---

# 本地 Obsidian 预览链接

本机经常同时开两个 vault。改聊天 UI 之前、用户说「没变化」时，先核对窗口，不要继续猜 CSS。

| 窗口 | 路径 | 必须链到 |
|---|---|---|
| ObsidianVault | `/Users/golddream/ObsidianVault` | 当前仓库 `dist/main.js` |
| Obsidian Sandbox | `~/Library/Application Support/obsidian/Obsidian Sandbox` | 当前仓库 `dist/main.js` |

Sandbox 曾链到 `.worktrees/feat-p-chat-motion`（旧 SoftAurora 包）。对着那个窗口改 develop，画面永远不变。

```bash
ls -l "<vault>/.obsidian/plugins/ratel-vault/main.js"
# 必须 → …/Ratel-CLI/dist/main.js
# 若 → …/.worktrees/… 则:
npm run link:vault -- "<vault>"
```

两个开着的窗口都要链。然后命令面板 **Reload app without saving**（只开新对话不够，V8 会缓存旧 `main.js`）。
